'use strict';

var ws = require('ws'),
    Peer = require('./Peer.js'),
    Network = require('./Network.js'),
    debug = require('debug')('p2ptv-server');

function Server(options) {
  var self = this;
}

Server.prototype = {

};

Server.prototype.constructor = Server;

module.exports = Server;
